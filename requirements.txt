pandas>=2.0
numpy>=1.24
scipy>=1.10
requests>=2.31
certifi>=2023.7.22
python-dotenv>=1.0
yfinance>=0.2.30
arch>=6.0
statsmodels>=0.14
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2
shap>=0.43
matplotlib>=3.7
seaborn>=0.12
numpy-financial>=1.0
pytest>=7.4

# OPTIONAL (heatmap portal layer, spec §4/§6 — pipelines gate on their presence):
# fastapi>=0.110       # `uvicorn api.server:app` serves the GeoJSON heatmap
# uvicorn>=0.29        # ASGI server for the API
# "psycopg[binary]">=3.1   # `python cli.py --market atlas --db` Postgres export
# redis>=5.0           # spec §1 parse-cache for api.server (mtime-keyed, no TTL)
